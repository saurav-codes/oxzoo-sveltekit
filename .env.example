# Read at request time by the server for the SSR line ($env/dynamic/private).
GREETING_TAG=change-me
# Baked into the client bundle at build time ($env/static/public); set before the first deploy.
PUBLIC_GREETING_TAG=change-me
